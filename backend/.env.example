ENV=dev
DEBUG=true

DATABASE_URL=postgresql://postgres:postgres@db:5432/stockanalyst

JWT_SECRET=8f2c9d1a6b4e7f9032ac18de56b7f41c9e2d5a7f3b8c1d4e
JWT_EXPIRE_MINUTES=10080

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
OAUTH_REDIRECT_URI=http://localhost:8000/api/auth/google/callback
FRONTEND_URL=http://localhost:3000

GEMINI_API_KEY=your-gemini-api-key-here
EMBEDDING_MODEL=text-embedding-004
EMBEDDING_DIM=768
CHAT_MODEL=gemini-2.5-flash

NEWS_REFRESH_INTERVAL_MINUTES=60
CHUNK_SIZE=800
CHUNK_OVERLAP=120

# Shared secret for the external free-tier cron trigger — see app/api/admin.py and
# .github/workflows/news-refresh.yml. Any random string; generate one with
# `openssl rand -hex 32` for a real deployment.
INTERNAL_REFRESH_TOKEN=change-me-to-a-random-value
